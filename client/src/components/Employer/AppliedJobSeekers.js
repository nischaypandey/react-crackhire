import React, { Fragment, useEffect, useState } from 'react';
import JobSeekerCard from './JobSeekerCard';

const AppliedJobSeekers = props => {
    useEffect(() => null);
    const [applied, setapplied] = useState([]);
    return (
        <div>
        </div>
    );
};

export default AppliedJobSeekers;
