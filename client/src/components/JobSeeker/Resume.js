import React from 'react';
import { Card } from 'react-bootstrap';

const Resume = props => {
    return (
        <Card>
          <Card.Header as="h4" className="text-center">Resume</Card.Header>
          <Card.Body></Card.Body>
        </Card>
    );
};

export default Resume;
